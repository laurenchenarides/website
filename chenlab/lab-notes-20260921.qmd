---
title: "Lab Meeting 9-21-26"
subtitle: "Getting started with Numerator: households, baskets, and sample coverage"
date: 2026-09-21
author: "Lauren Chenarides"
toc: true
toc-location: left
toc-depth: 2
---

## Today’s goals

By the end of this lab, you should be able to connect R to the Numerator DuckDB database on Orchard, read a small household or basket extract, close the connection, and explain which households your analysis includes. We will begin at the **household and basket level**; product-level work comes later.

| Time | Activity |
|---|---|
| 2:00–2:30 | Project updates, access checks, and questions |
| 2:30–2:40 | Read the ER diagram and identify our starting tables |
| 2:40–2:55 | Connect R, inspect columns, and read a small extract |
| 2:55–3:10 | Static membership, spells, gaps, and attrition |
| 3:10–3:20 | Projection factors and expenditure comparisons |
| 3:20–3:30 | Start the sample-flow homework |

## Files for this lab

Dr. Chenarides will provide the following files in the designated lab folder on Orchard. They are not distributed through the public website or GitHub repository. Copy `config.R` and `read_numerator.R` into your own R project on Orchard for the examples below.

- `config.R`: open and close a read-only connection.
- `read_numerator.R`: inspect columns and retrieve a small extract.
- `02_static_household_spells.sql`: instructor setup for the shared household reference tables.
- `03_sample_flow.sql`: student diagnostic exercise.

These are teaching versions of the earlier scripts. They use `core_new` consistently, do not depend on a saved metadata table, and do not run the older plant-based project's spending/activity filters. The supplied ER diagram, dictionary, scripts, and internal draft documentation are private lab materials; access them on Orchard during the discussion.

::: {.callout-important title="Instructor preparation"}
Before class, verify that `core_new` points to the intended release and that its parquet files are available on Orchard. The reviewed setup script identifies this as the July 2026 delivery, with purchases through June 30, 2026; verify the actual date ranges in the database.

Run `02_static_household_spells.sql` once in a DBeaver SQL editor attached to that database. It requires `core_new.static_table`, already created by the corresponding section of `00_setup.sql`. Students do **not** need to rerun the full setup or `01_household_diagnostics.sql`. Stop on SQL errors and issue `ROLLBACK` before retrying. Rebuild the reference tables after a data refresh.

Coordinate this write with other users: disconnect R readers first, then disconnect the DBeaver write connection before the class connects. Multiple direct DuckDB readers can share the file when all connections are read-only. See the [DuckDB concurrency documentation](https://duckdb.org/docs/current/connect/concurrency).
:::

## Read the ER diagram

An **entity-relationship (ER) diagram** shows tables, their fields, and the keys connecting them. Start with the **grain**: what does one row represent? Then identify the join key and whether one record can match several records in the other table. A `1` means one; `*` means many. A line suggests a relationship, but we must still check uniqueness in the actual data.

For this lab, use this simplified map alongside the supplied diagram:

```{mermaid}
flowchart LR
  P["people_table<br/>one household"] -->|user_id| B["summarylvl_fact_table<br/>one basket"]
  C["banner_table<br/>one retailer banner"] -->|banner_id| B
  H["static_households<br/>one household"] -->|user_id| B
  H -->|user_id| S["static_household_spells<br/>one household + spell"]
  M["static_household_months<br/>one household + endpoint month"] --> S
```

The three `static_household...` objects are lab-created references, not vendor tables. The arrows show how to relate them; they are not instructions to join every object at once.

### Start with these tables

| Table | Grain and purpose | Key fields |
|---|---|---|
| `core_new.people_table` | One current household record; home geography | `user_id`, `postal_code`, `census_region_name` |
| `reference.static_households` | One household appearing in the supplied static file; safe household-level join | `user_id`, `is_static_household`, observed bounds, `n_spells` |
| `reference.static_household_spells` | One consecutive run of monthly static-file coverage per household | `user_id`, `spell_id`, spell bounds, gap length |
| `core_new.summarylvl_fact_table` | One basket/trip; our main transaction table | `basket_id`, `user_id`, `banner_id`, `transaction_date`, spending and order fields |
| `core_new.banner_table` | One banner; add when interpreting retail channel | `banner_id`, `banner`, `channel`, `parent_channel` |

**Reference tables** describe or classify observations. **Transaction tables** record purchases. The `reference` schema holds our derived lab references; vendor lookup tables such as `people_table` and `banner_table` remain in `core_new`. A schema is a namespace, not a guarantee of a table's grain.

`postal_code` is the household's current home postal code. `store_postal_code` is the store's postal code. They answer different geographic questions. Preserve both as text so leading zeros survive. For historical residence, the dictionary describes `people_history_table.FROM_DATE` and `UNTIL_DATE` as **demographic validity dates**, not enrollment dates. We will need a validated time-varying join for that work.

The basket table includes `basket_total` (receipt total), `basket_sub_total` (pre-tax subtotal), `basket_adjusted_subtotal`, transaction dates, store geography, and `order_method_type`, `order_provider`, `delivery_method_type`, `delivery_provider`, and `order_delivery_type`. Choose an expenditure measure and document it. Retail channel comes from `banner_table`; ordering and fulfillment describe a different dimension. A grocery-store basket is not necessarily all food.

Before joining, check that the household and banner references have unique keys. The homework checks the candidate basket key `(basket_id, user_id, banner_id, transaction_date)`. Joining baskets to all spells using only `user_id` repeats each basket for households with multiple spells. Use the one-row household view for household labels, or an explicit month match for monthly coverage. Item rows also repeat basket totals, which is why we start with the summary table.

## Connect R to DuckDB

**DBeaver is a database client. R connects directly to the same DuckDB file; it does not read DBeaver's screen or export a DBeaver table.** Run R/RStudio inside your Orchard session. In DBeaver's connection settings, find the database file path and copy it, using forward slashes in R. The file may have no extension. Its views may refer to parquet files elsewhere on Orchard, so copying only the database to a laptop is not enough.

Install the packages once into your own R library:

```r
install.packages(c("DBI", "duckdb"))
```

At the start of each session, set the actual path and source the helpers:

```r
# Replace this example with the database file shown in DBeaver.
Sys.setenv(NUMERATOR_DB_PATH = "D:/path/to/numerator.duckdb")
source("config.R")
source("read_numerator.R")

con <- open_numerator_connection()
describe_numerator(con, "people_table")
describe_numerator(con, "summarylvl_fact_table")
```

The path is supplied per user; no personal path is built into the scripts. Opening is read-only and fails if the database file does not exist. Use one lab connection at a time; the close helper disconnects and shuts down its database instance to release the file lock. If a file-lock error appears, check whether DBeaver or another process still has a write connection. Use the [DuckDB R client documentation](https://duckdb.org/docs/current/clients/r) for connection details.

Read selected columns and a small preview:

```r
households <- read_numerator(
  con, "people_table",
  columns = c("user_id", "postal_code", "census_region_name"), limit = 10
)

spells <- read_numerator(
  con, "static_household_spells", schema = "reference", limit = 10
)

baskets <- read_numerator(
  con, "summarylvl_fact_table",
  columns = c("basket_id", "user_id", "banner_id", "transaction_date",
              "basket_total", "store_postal_code", "order_method_type",
              "delivery_method_type", "basket_er_factor"),
  where = "transaction_date >= ? AND transaction_date < ?",
  params = list(as.Date("2025-01-01"), as.Date("2025-02-01")),
  limit = 100
)

close_numerator_connection(con)
```

The helpers cast identifiers to character **inside DuckDB**, before R can round a large `DECIMAL(38,0)` value, and return lowercase column names. A `LIMIT` preview is not a random or representative sample. The independently previewed tables need not contain matching households. For analysis, filter or aggregate in SQL first; even a one-month extract can be large. `limit = NULL` deliberately collects all rows matching your condition.

For a reusable script, put the work inside a function so the connection closes even if a query fails:

```r
run_lab <- function() {
  con <- open_numerator_connection()
  on.exit(close_numerator_connection(con), add = TRUE)
  DBI::dbGetQuery(con, "
    SELECT date_trunc('month', transaction_date)::DATE AS month,
           COUNT(*) AS basket_rows, COUNT(DISTINCT user_id) AS households
    FROM core_new.summarylvl_fact_table
    WHERE transaction_date >= DATE '2025-01-01'
      AND transaction_date < DATE '2026-01-01'
    GROUP BY 1 ORDER BY 1
  ")
}
monthly_counts <- run_lab()
```

Only the small result enters R. The database does the scan and aggregation.

## Static households and participation spells

“Static” generally refers to a sample selected for sustained reporting over a specified period. It does **not** mean that a household never moves, never changes, or participates forever.

Our internal draft describes Numerator's official criteria in terms of consecutive reporting, qualifying receipts, demographic balance, and transcription completeness. It also says the relationship between those criteria and membership in `static_table` is unresolved. The older diagnostics implement a separate project proxy: at least 36 active months, at least 26 receipts, and a household-size spending floor, using gas-excluded activity in selected channels. **Active months are not necessarily consecutive months.** That proxy is not an official vendor classification.

::: {.callout-important title="The operational definition for this exercise"}
For this lab, `is_static_household = 1` means that the household appears in the supplied `core_new.static_table`. Call it **static-file membership** in your homework. This is not a verified official static-panel flag or proof of continuous reporting. The reference contains only members; use a left join from all observed households when comparing members and nonmembers.

The dictionary defines `START_DATE` and `END_DATE` as the endpoints of a 12-calendar-month static period. We index each record by the **month of `END_DATE`**, the trailing window's endpoint, and identify consecutive runs of those monthly records. This labels weighting coverage; it does not establish that this is the correct month-matching rule for applying household weights to transactions.
:::

The SQL first reduces the source to one record per `user_id` and endpoint month, recording duplicate source-row counts for review. It then starts a new spell whenever at least one endpoint month is missing. It does **not** fill all months inside every overlapping 12-month window, which would hide gaps between weighting records.

Consider a fictional household with endpoint months January, February, March, June, and July:

| Spell | First observed month | Last observed month | Months in spell | Missing months before spell |
|---|---|---|---:|---:|
| 1 | January | March | 3 | 0 |
| 2 | June | July | 2 | 2 |

This household has two observed coverage spells and returns after an April–May gap. Its overall January–July span contains five observed months, not seven continuously observed months. A single spell can still cover only a short portion of the full release.

The reference exposes `observed_entry_month`, `observed_last_month`, `spell_start_month`, `spell_end_month`, `n_spells`, `gap_months_before`, and `is_return`. The actual `panel_entry_date` and `panel_exit_date` remain **NULL** because the reviewed files do not establish them. `touches_release_start` and `touches_release_end` flag spells that reach the available static-file boundaries; activity may extend outside what we can observe.

A missing receipt is not proof that a household left the panel, and disappearance from the weighting file is not proof that it stopped purchasing. Returns may reflect renewed reporting or weighting eligibility. Attrition and changing sample composition can affect expenditure trends, event studies, and duration models. Compare unbalanced and continuously covered samples, but recognize that requiring future coverage can select survivors. Keep recent reporting lags in mind; low activity at the release boundary may reflect incomplete collection. Do not convert these gaps into verified enrollment events.

## Projection factors and expenditure comparisons

The supplied dictionary describes the fields' intended roles:

| Field | Where it lives | Intended role |
|---|---|---|
| `ER_FACTOR` | Item-level facts | Extended-reach projection beyond the observed panel |
| `BASKET_ER_FACTOR` | Basket-level facts | Extended-reach projection of basket purchases |
| `TREND_FACTOR` / `BASKET_TREND_FACTOR` | Corresponding fact table | Trend-adjusted projection |
| `DEMO_WEIGHT` / `NATIONAL_FACTOR` | `static_table` | Demographic weighting / national scaling for the static reference population |

::: {.callout-warning title="What we do not yet know"}
We do **not** yet have documentation explaining how Numerator constructs these weights: calibration targets, normalization, trimming, eligible populations, or how the factor families relate. Field descriptions explain intended use, but do not verify a national estimator for a chosen subset. We must confirm the applicable spending measure, population, period, and whether any factors combine before presenting national expenditure estimates.
:::

The basic projection calculation to discuss is a sum of expenditures times their corresponding factor, **if Numerator confirms that factor is the appropriate standalone multiplier**:

$$
\widehat{E}_t = \sum_{b \in \mathcal{B}_t} E_b\,w_b,
\qquad w_b = \text{BASKET\_ER\_FACTOR}_b.
$$

For example, fictional baskets of $20 and $30 with factors 100 and 200 produce a raw total of $50 and an illustrative projected sum of $8,000. Dividing that sum by $100 + 200$ gives a weighted **mean basket expenditure**, not national total expenditure or expenditure per household. At the item level, the analogous calculation would use item spending and `ER_FACTOR`; do not put an item factor on repeated basket totals.

Before calculating, inspect missing, nonfinite, zero, and negative factors, missing expenditures, and duplicate baskets. Do not replace missing factors with 1 or silently report a sum based on only the valid rows. Negative spending may represent returns and needs its own interpretation. This example reports factor coverage and returns an illustrative sum only when every basket has a finite positive factor and nonmissing spending:

```sql
SELECT
    date_trunc('month', transaction_date)::DATE AS month,
    COUNT(*) AS n_baskets,
    COUNT(*) FILTER (WHERE basket_total IS NULL) AS missing_spending,
    COUNT(*) FILTER (
        WHERE basket_er_factor IS NULL OR NOT isfinite(basket_er_factor)
           OR basket_er_factor <= 0
    ) AS invalid_factors,
    SUM(basket_total) AS observed_spending_nonmissing,
    CASE WHEN COUNT(*) FILTER (
        WHERE basket_total IS NULL OR basket_er_factor IS NULL
           OR NOT isfinite(basket_er_factor) OR basket_er_factor <= 0
    ) = 0 THEN SUM(basket_total * basket_er_factor) END
        AS illustrative_er_weighted_sum
FROM core_new.summarylvl_fact_table
WHERE transaction_date >= DATE '2025-01-01'
  AND transaction_date < DATE '2026-01-01'
GROUP BY 1 ORDER BY 1;
```

Run the homework's duplicate-key check first. This illustration uses the full basket population in the date window, not the static/ZIP-filtered homework subset. Restricting to static-file members, known ZIPs, or continuously observed households changes the represented sample; supplied factors may not project that restricted sample nationally. Do not multiply ER, trend, demographic, and national factors together without explicit instructions from Numerator.

For national comparisons, align time periods, population, channels, tax treatment, and expenditure scope with the benchmark. Basket totals from grocery retailers cannot stand in for food spending without resolving nonfood purchases. Report raw counts and unweighted spending beside any projected calculation, examine changes in sample composition and missing weights, and distinguish nominal changes from inflation-adjusted changes. Weighting alone does not establish representative ZIP coverage or correct all attrition bias.

## Homework: build the first sample-flow diagnostics

Start with `03_sample_flow.sql`. It uses temporary tables, so run it in one connection; they disappear when you disconnect. Use **calendar year 2025** initially, after checking that the source covers it. The end date is exclusive (`2026-01-01`). Keep the same period at every step and record the release used.

**All observed households → static-file members → geographic coverage → coverage over time.**

1. **All observed households:** Count distinct nonmissing `user_id` values with a basket in the analysis window, across all channels, including gas-only baskets. This is an observed purchaser population, not every enrolled household or every row of `people_table`. Report missing IDs and dates separately.
2. **Static-file members:** Among those households, retain membership in at least one static endpoint month within the same analysis window. Do not treat membership somewhere else in the full release as membership this year. Report retained counts, losses, and shares of the starting sample.
3. **Geographic coverage:** Left join current `people_table` geography. Report missing household records, missing postal codes, and other postal formats separately. Count households and distinct ZIPs by region. The provided five-digit/ZIP+4 check establishes only a ZIP-shaped string; it does not validate a ZIP against a geographic crosswalk or measure population coverage.
4. **Coverage over time:** Plot monthly observed households, households with same-month static-file coverage, and those also passing the ZIP-format screen. Retain zero-count months. Compare static members with and without an observed basket that month, and show the distribution of spell counts. Also report the additional loss from requiring static coverage in every analysis month as a sensitivity check.

**Bring to the next lab:** one sample-flow table, one geography summary, one monthly line plot, and a short paragraph stating your inclusion rules and explaining a gap/return example. Include the SQL/R needed to reproduce them. Identify the largest source of sample loss and one reason it could affect your research question. Discuss whether a changing monthly count reflects purchase activity, weighting coverage, reporting lag, or something the available fields cannot distinguish.

The exercise deliberately keeps the number of basket-active months separate from the number of static-coverage months. **No observed basket is not automatically zero spending.** Homework counts are unweighted; national projection is a separate question. Keep household extracts and detailed geographic outputs on Orchard or other approved lab storage.

## Source notes and questions to resolve

These notes use the supplied ER diagram, `numerator_variable_metadata_new.csv`, the master data dictionary, the internal documentation draft, and the earlier R/SQL scripts. Where the dictionary and draft differ, the lab makes the uncertainty explicit: the dictionary identifies demographic validity periods, while the older draft still treated their meaning as an open question. The live DuckDB schema is the authority for loaded column types.

Before using these data for final estimates, resolve the official static-panel flag, the interpretation of absent weighting months, the correct time alignment of household weights, and the construction/application of each projection factor. The lab's spell table is an auditable coverage reference that can be revised when those answers arrive.
